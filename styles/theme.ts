import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#dcf2e9",
      main: "#19aa6e",
    },
    secondary: {
      main: "#402d28",
      light: "#eae0d5",
    },
  },
});

export default theme;
